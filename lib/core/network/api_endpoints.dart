class ApiEndpoints {
  ApiEndpoints._();

  static const String baseUrl = 'https://api.test.gain.io';

  static const String appLogin = '/auth/app-login';

  /// GraphQL endpoint path (relative to [baseUrl]).
  static const String graphQL = '/graphql';
}
